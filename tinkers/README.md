# Local experiments

Auto-Tinker creates generated experiments below this directory. Each child is an independent, nested Git repository with its own history and optional remote.

Everything below `tinkers/` is ignored by the Auto-Tinker product repository except this explanation file. New experiment remotes are private by default, and deleting or moving a local checkout does not delete its Markdown knowledge record under `.auto-tinker/`.
